<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hdu.homemart">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MartListActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".UpDelActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".UserMartListActivity" />
        <activity android:name=".UserOrderActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".DeliveryListActivity"></activity>
    </application>

</manifest>